npm run build 生成 all/common/simple 三种体积档并压缩；npm run release 依次执行 build:lib → build:i18n → build → build:esm → build:extension → build:ssr 完成全量发布；npm run dev 同时启动 i18n 增量构建与 http-server 打开 test 目录；npm run test:visual 运行 Puppeteer 视觉回归，npm run test 运行 Jest 单测。